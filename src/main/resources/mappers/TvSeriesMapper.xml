<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rottentomatoes.movieapi.mappers.TvSeriesMapper">

    <sql id="tvSeriesSelectSql">
        tv_series.id,
        tv_series.title,
        tv_series.description,
        tomatometer_info_tv.tomatometer value, /* TODO enforce tomatometer=null when state=NA */
        CASE
            WHEN tomatometer_info_tv.certified_fresh THEN 'certified_fresh'
            WHEN tomatometer_info_tv.num_reviews &lt; 5 THEN 'NA'
            WHEN tomatometer_info_tv.tomatometer IS NULL THEN 'NA'
            WHEN (tomatometer_info_tv.tomatometer >= 60) THEN 'fresh'
            ELSE 'rotten'
        END AS state

        FROM tv_series
        LEFT JOIN tomatometer_info_tv
        ON tomatometer_info_tv.media_type='SR'
        AND tomatometer_info_tv.media_id=tv_series.id

    </sql>

    <select id="selectTvSeriesById" resultMap="tvSeriesResultMap" parameterType="map">
        SELECT
        <include refid="tvSeriesSelectSql"/>
        WHERE tv_series.id = #{id}
    </select>

    <resultMap id="tvSeriesResultMap" type="com.rottentomatoes.movieapi.domain.model.TvSeries">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="description" column="description"/>
        <association property="tomatometer" javaType="map">
            <result property="value" column="value"/>
            <result property="state" column="state"/>
        </association>
    </resultMap>

</mapper>